# Build Log

Append-only. One entry per session, newest last. Format:

```
## YYYY-MM-DD — <session focus>
- Done: <what shipped, with branch/PR>
- Decisions: <anything locked, with why>
- Blocked/Open: <blockers, escalations, questions for Giahy>
- Next: <the single most important next action>
```

## 2026-07-05 — Infrastructure bootstrap (Fable 5 + Giahy)
- Done: repo created; HANDOFF.md, CLAUDE.md, TASKS.md, agent roster (6 agents, agency-agents base + Sushi Sea Protocol); PRD copied to docs/; `dev` branch created
- Decisions: dedicated repo (PRD §13 Q1 = B) · PRD §6 sequencing honored · Sonnet orchestrator/workers, Haiku for mechanical tasks, Opus advisor escalation-only · comments stay PRD §8 (reasoning in PRs/commits) · merge gate: auto to dev on CI+2 reviews, Giahy gates main · Figma is the UI tool
- Blocked/Open: M0 cook verb (Giahy grill-me, blocks vertical slice) · branch protection = Giahy manual step · mem0 MCP not connected in bootstrap session — decisions logged here instead
- Next: Wave 1 — M1 toolchain skeleton (`dev-systems`)

## 2026-07-28 — Vault→repo migration (Opus 5 + Giahy)
- Done: staged scaffold moved out of the MIMIR vault into this repo (`HANDOFF`, `CLAUDE`, `ROADMAP`, `TASKS`, `BUILD_LOG`, `README`, 6 agents, `docs/PRD.md`); `.mcp.json` declaring mem0; mem0 protocol bullet added to all 6 agents; `scripts/sync-prd.sh` + `.gitignore`; vault `repo-staging/` deleted
- Decisions: `docs/PRD.md` is a one-way mirror of the vault PRD, enforced by a sync script rather than by discipline — the staged copy had already drifted (it still claimed the repo-layout question was open after §10/§13 Q1 were resolved on 2026-07-05) · mem0 declared at repo root so subagents inherit it, not per-agent
- Blocked/Open: `MEM0_API_KEY` unset in the migration environment, so mem0 tools were unavailable and no memories were written — Giahy must set it as an environment secret · `dev` branch does not exist yet
- Next: Giahy setup checklist (dev branch, MEM0_API_KEY, branch protection), then M0 cook-verb grill-me — still the only thing blocking the vertical slice

## 2026-07-28 — M1 toolchain skeleton (Sonnet 5, dev-systems)
- Done: `claude/sushi-m1-toolchain` off `origin/dev`. `default.project.json` mapping PRD §7.1 exactly (ServerScriptService/{Services,Bootstrap}, ServerStorage/Modules, ReplicatedStorage/{Config,Modules,Events/{RemoteEvents,RemoteFunctions}}, StarterPlayer/StarterPlayerScripts/{Controllers,UI}, StarterGui) onto `src/{server,shared,client,gui}` per §10; `rokit.toml` pinning rojo 7.7.0, wally 0.3.2, selene 0.31.0, stylua 2.5.2, lune 0.10.5; `wally.toml`/`wally.lock` (zero deps); `selene.toml` (std="roblox"); `stylua.toml`; all 21 scaffold files from §7.1 (6 services + Bootstrap + 5 ServerStorage modules + 2 Config + 1 shared module + 3 controllers + 3 UI) each with a role-header comment only, no logic; `tests/placeholder.spec.luau` (Lune, requires 3 real ServerStorage modules, asserts they return tables); `.github/workflows/ci.yml` (rokit install --no-trust-check → selene update-roblox-std → rojo build → selene → stylua --check → lune run tests). Verified locally end-to-end: downloaded rojo/selene/stylua/lune/wally/rokit binaries into the sandbox (no system installs available), ran `rojo build` (clean, correct tree — inspected the .rbxlx), `selene src` (0 errors/warnings), `stylua --check src` (clean), `lune run tests/placeholder.spec.luau` (passes), `wally install` (0 packages, manifest valid), and `rokit install --no-trust-check` against the real `rokit.toml` (fetched all 5 pinned versions correctly). CI itself has not run on GitHub Actions yet — only pushed, not yet observed green in the Actions tab
- Decisions: renamed PRD §7.1's `Init.server.lua` to `Bootstrap.server.lua` — Rojo treats a literal `init.server.lua` (case-insensitive on Windows) as a magic filename that collapses its entire containing folder into a single Script, which folded `ServerScriptService` itself into a Script and broke the `Services/` subtree; confirmed by building both ways and diffing the output `.rbxlx`. Same role, same "wires services, no logic here" contract, different filename to dodge the Rojo convention · added `lune` as a 5th rokit-pinned tool beyond the 4 named in the task brief (rojo/wally/selene/stylua) — deliverable also requires a headless test runner, and Lune (zero Wally deps, cross-platform, plain-Luau `require()`) was the only zero-dependency option consistent with §9's "prefer zero dependencies until one is justified"; TestEZ would have needed a Wally package · CI runs on `ubuntu-latest` and installs the toolchain via direct rokit binary download + `rokit install --no-trust-check` rather than a third-party `setup-rokit` marketplace action, since no action under the `rojo-rbx` org is official/canonical yet and this keeps the exact-version guarantee self-contained · selene's Roblox std isn't vendored in-repo (it's a large generated file cached per-machine); CI regenerates it every run via `selene update-roblox-std` before linting, same pattern used by other Rojo/selene repos · kept PRD's literal `.lua` extensions for all named files (not `.luau`) since §7.1/§7.3/§8's code fences are consistent and explicit on `.lua`, even though the task brief said ".luau files" generically
- Blocked/Open: none — no genuine ambiguity hit beyond the Init.server.lua rename, which was a toolchain-forced, low-risk, reversible call within the systems lane rather than a design question, so it wasn't escalated. Flagging for Giahy/reviewer-code anyway since it's a literal deviation from PRD §7.1's ASCII layout · CI has not actually been observed green on GitHub Actions (no `gh` CLI/token in this environment to watch the run) — first push will be the real test · `docs/PRD.md` and `ROADMAP.md` are identical between `origin/dev` and the other checkout in this environment, but `HANDOFF.md` differs (dev is missing the 2026-07-28 mem0-plugin-migration and Roblox Studio MCP updates that are on `main`/other branches) — not fixed here, out of scope for M1, but worth a docs-sync pass before those branches merge into `dev`
- Next: open a PR `claude/sushi-m1-toolchain` → `dev`, confirm CI is actually green on GitHub Actions (only locally verified so far), then `reviewer-code`/`reviewer-reality` before M2 (`PlayerDataService`, depends on M1) starts
