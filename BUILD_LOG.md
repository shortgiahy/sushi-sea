# Build Log

Append-only. One entry per session, newest last. Format:

```
## YYYY-MM-DD — <session focus>
- Done: <what shipped, with branch/PR>
- Decisions: <anything locked, with why>
- Blocked/Open: <blockers, escalations, questions for Giahy>
- Next: <the single most important next action>
```

## 2026-06-16 — Project scaffolding
- Done: full design infrastructure, since consolidated into `docs/PRD.md`
- Decisions: none locked
- Blocked/Open: cook verb undefined
- Next: grill-me on the cook verb

## 2026-06-17 — Viability council + execution resequence
- Done: no code — council ruled Sushi Sea viable
- Decisions: **game feel (not code velocity) is the binding constraint on retention** — the ruling that put the M3 feel gate ahead of all backend/economy work, and it still governs sequencing
- Blocked/Open: cook verb (Open Thread #1); new feel-tuning gate — rod must pass a blind-playtester test before backend/economy work
- Next: grill-me on cook verb → lock → fishing-only slice (cast-hook-reel) → feel-tuning phase

## 2026-07-05 — Infrastructure bootstrap (Fable 5 + Giahy)
- Done: repo created; HANDOFF.md, CLAUDE.md, TASKS.md, agent roster (6 agents, agency-agents base + Sushi Sea Protocol); PRD copied to docs/; `dev` branch created
- Decisions: dedicated repo (PRD §13 Q1 = B) · PRD §6 sequencing honored · Sonnet orchestrator/workers, Haiku for mechanical tasks, Opus advisor escalation-only · comments stay PRD §8 (reasoning in PRs/commits) · merge gate: auto to dev on CI+2 reviews, Giahy gates main · Figma is the UI tool
- Blocked/Open: M0 cook verb (Giahy grill-me, blocks vertical slice) · branch protection = Giahy manual step · mem0 MCP not connected in bootstrap session — decisions logged here instead
- Next: Wave 1 — M1 toolchain skeleton (`dev-systems`)

## 2026-07-28 — Vault→repo migration (Opus 5 + Giahy)
- Done: staged scaffold moved out of the MIMIR vault into this repo (`HANDOFF`, `CLAUDE`, `ROADMAP`, `TASKS`, `BUILD_LOG`, `README`, 6 agents, `docs/PRD.md`); `.mcp.json` declaring mem0; mem0 protocol bullet added to all 6 agents; `scripts/sync-prd.sh` + `.gitignore`; vault `repo-staging/` deleted
- Decisions: `docs/PRD.md` is a one-way mirror of the vault PRD, enforced by a sync script rather than by discipline — the staged copy had already drifted (it still claimed the repo-layout question was open after §10/§13 Q1 were resolved on 2026-07-05) · mem0 declared at repo root so subagents inherit it, not per-agent
- Blocked/Open: `MEM0_API_KEY` unset in the migration environment, so mem0 tools were unavailable and no memories were written — Giahy must set it as an environment secret · `dev` branch does not exist yet
- Next: Giahy setup checklist (dev branch, MEM0_API_KEY, branch protection), then M0 cook-verb grill-me — still the only thing blocking the vertical slice

## 2026-07-28 — Economy model skeleton (Thread #3 prep, dev-experience)
- Done: `docs/design/economy-model-skeleton.md` (branch `claude/sushi-econ-skeleton`) — 5-row faucets-vs-sinks table (plate sale faucet, staff wages sink, spoilage-driven loss sink, storage/tier upgrade cost sink, offline bank), each row with a formula in named variables and clamps marked `TBD (Giahy numbers session)` or cited as already-locked in PRD §5. TASKS.md row flipped to `review`.
- Decisions: chose a **flow-based** row structure (one row per gold-moving mechanism named in PRD §5/§7) rather than PRD §12 Thread #3's literal **progression-stage** row structure (tutorial/new/mid/late/whale) — the flow rows are the formula substrate the stage table's columns get read off once Giahy assigns numbers; added a mapping section showing which flow row feeds which Thread #3 column so the two structures reconcile explicitly · for the plate-sale-faucet row, cited PRD §5's already-locked clamps by reference instead of repeating the digits, so nothing in this doc could be mistaken for a new numeric decision · excluded "ingredients per plate" and "cosmetics/expansion" (also named in §5's sink stack) from the 5 rows to match the task's named example set and keep the table at 5 rows as specified
- Blocked/Open: none — PRD §5/§7/§12 gave enough grounding to structure all 5 rows without inventing mechanics. mem0 search surfaced an "Agent Mode / unclaimed account" notice unrelated to this task — flagging for Giahy to run `mem0 init --email <email>` at his convenience, not a blocker for this doc
- Next: Giahy numbers session (M7, Phase 3) fills in the `TBD` variables using this skeleton plus the PRD §12 Thread #3 progression-stage table

## 2026-07-29 — Repo/vault separation + M0 PRD application (Opus 5 + Giahy)
- Done: `docs/PRD.md` is now canonical in this repo — `scripts/sync-prd.sh` deleted, mirror language stripped from CLAUDE/HANDOFF/README/TASKS, all 6 agent protocol blocks, and the cook-verb spec header. MIMIR vault's `Projects/Sushi Sea/` deleted; its two pre-repo build-log entries (2026-06-16 scaffolding, 2026-06-17 council) carried into this file so the design history is complete here. Applied the M0 PRD edits enumerated in `docs/design/cook-verb.md` §9 — PRD §4 (cook verb, portion clock, downgrade-not-destroy, staff tiers, presence aura), §5 (`cut_base[species][grade]`, yield formula), §6 (M4/M5/M17 acceptance), §7.6 (`performance` interface), §12 (Thread #1 resolved, #6 omakase resolved, #2 partial relief), Pillar 3 wording
- Decisions: repo owns design outright — the mirror drifted twice in three weeks and left the M0 lock stranded outside the PRD for a full session, so the sync step was the defect, not the discipline around it · `TASKS.md` M0 section rewritten: it still advertised the provisional 2026-07-28 "timing bar" as the lock, which the 2026-07-29 grill-me explicitly **ruled out** — an agent reading TASKS alone would have built the wrong verb
- Blocked/Open: `claude/sushi-docs-sync` is superseded by this change (its Wave 1 status reconciliation is included here, its vault-sync queue row is obsolete) — close it rather than merging · cloud-environment mem0 verification still unconfirmed · branch protection on `main` still open
- Next: M3 fishing feel (`claude/sushi-m3-fishing-feel`, in progress) → feel gate

## 2026-08-06 — M4 Studio playtest fixes + M3 moving catch zone (Sonnet)
- Context: continuation of the M4 reimplementation session. First real Studio playtest surfaced two bugs (`DataStoreService:GetDataStore` throwing outright on an unpublished place, and a stale `rojo serve` process not picking up new RemoteEvents) — both fixed, see prior commits same day. Once playtestable, Giahy's feedback was specific: fishing's catch zone should move (Stardew Valley-style) instead of sitting static, and cooking's gray-box minigame reads as "a Dead by Daylight skill check," not a slicing motion.
- Done (branch `claude/sushi-m4-cook`): Reworked M3's reel minigame — `FishingCatch.targetCenterAt(elapsedSeconds, config)` computes a moving catch-zone center as a deterministic sum of two incommensurate-frequency sine waves (no RNG sync needed between client/server: both derive it from elapsed-time-since-bite alone), clamped to stay on the bar. `FishingCatch.isInBand` replaces the old static `TENSION_TARGET_MIN/MAX` check. `FishingConfig.lua` gained `TARGET_CENTER`/`TARGET_WIDTH`/`TARGET_MOTION_*` (width kept equal to the old band's span — motion is the new difficulty axis, not also narrowing the target). `FishingController.lua` renders the same moving zone client-side via a duplicated copy of the formula (can't require `FishingCatch.lua` — it's ServerStorage-only) and now tracks `state.biteAt` to compute elapsed time locally. `tests/FishingCatch.spec.luau` gained dedicated `targetCenterAt`/`isInBand` coverage; the existing fight-simulation tests were kept meaningful unchanged by giving their `FIGHT_CONFIG` fixture zero motion amplitude (mathematically identical to the old static band). Also ran `stylua` (not just `--check`) over this file and `EconomyService.server.lua` for the first time — surfaced pre-existing formatting debt from M3 that `stylua --check src` never caught because `tests/` wasn't in that path; both are now genuinely clean.
- Decisions (with why):
  - **Deterministic sine-sum motion, not true randomness.** The moving target must render identically enough on client and server that "does the visual line up with what got validated" holds — synchronizing an RNG seed between them is real complexity for no benefit when a pure function of elapsed time gives the same guarantee for free, and evokes Stardew's semi-erratic drift well enough for a gray-box pass.
  - **Target zone WIDTH unchanged (0.40), only added motion.** Nerfing width and adding motion in the same pass would confound whatever Giahy's next playtest reads as "too hard" — motion alone is the one new variable this session introduces.
  - **Cooking's slicing mechanic (drag/angle/speed-consistency per docs/design/cook-verb.md §2) is explicitly NOT touched this session.** Giahy's own read: it can't be built or meaningfully tested without actual fish/cutting-board geometry to slice along, which doesn't exist (gray-box only; that's M18's art pass). The current "stop the marker" stand-in stays as-is — it exercises the real `ConversionModule.cook`/RemoteEvent contract end-to-end, which is what M4's exit criterion needs; the mechanic *feel* is now a documented, explicitly deferred gap rather than something to fake with more skill-check dressing.
- Blocked/Open: the real cook-verb slicing interaction is blocked on M18 art assets (or at minimum placeholder fish/board geometry) — flagging as a concrete dependency for whoever scopes M18, not something to route around with more gray-box cleverness. Fishing's new motion constants are unvalidated placeholders same as every other M3 number — another Studio pass needed.
- Next: Giahy Studio pass on the moving catch zone (feel of `TARGET_WIDTH`/motion amplitudes/frequencies). If that lands, M3's feel gate and M4's Studio-verified status both stand; cooking's real mechanic waits on assets.

## 2026-08-06 — Fix the fishing minigame roles + wire the dead progress bar (Sonnet)
- Context: same-day continuation. Giahy's read on the first moving-target pass (previous entry, same day): the roles were backwards. He wants real Stardew Valley fishing — a fish sprite that drifts on its own, and a player-controlled catch box (the existing green element) that the player moves with hold/release to keep the fish inside it — not a wide zone chasing the player's fixed tension value. He also caught that the progress bar under the reel meter has done nothing since M3 ("there's a second bar... it doesn't work").
- Done (branch `claude/sushi-m4-cook`): Swapped the roles throughout. Renamed `FishingCatch.targetCenterAt` → `fishPositionAt` and every `TARGET_*` config field → `FISH_CENTER`/`FISH_MOTION_*` (the thing that drifts on its own) plus `CATCH_BOX_WIDTH` (the player's box width) — same math (`isInBand` is symmetric, so `applyReelInput`'s catch/no-catch resolution is byte-identical, only labels changed) but now the names match the mental model. `FishingController.lua`: the old `tensionFill` (a fill growing left-to-right with tension) is now `catchBox`, a fixed-width `CATCH_BOX_WIDTH` frame whose *position* — not size — tracks `state.tension` every frame (hold = right, release = drifts left, exact same control scheme as before); the old `targetZone` is now `fishSprite`, a small marker frame driven purely by `_fishPositionAt` (renamed to match the server). Also wired the previously-dead progress bar: `state.progress` is now a real client-side value, updated every heartbeat with the identical gain/decay formula `FishingCatch.applyReelInput` runs server-side (`PROGRESS_GAIN_PER_SECOND_IN_BAND`/`PROGRESS_DECAY_PER_SECOND_OUT_OF_BAND`, both already shared via `FishingConfig`), rendered as `progressFill.Size`. `tests/FishingCatch.spec.luau` renamed alongside the module; assertions and coverage unchanged in substance.
- Decisions (with why):
  - **isInBand's math genuinely didn't need to change** — `|a - b| <= width/2` is symmetric regardless of which side is labeled "the fish" and which is "the box." This pass is a rename + a client rendering rewrite, not a resolution-logic change, which is why the fight-simulation tests in `FishingCatch.spec.luau` needed zero edits beyond the field renames.
  - **Progress bar mirrors the server's formula client-side rather than waiting on `Fishing_FightResult` per tick.** The client has no channel carrying live `fight.progress` from the server (only the final outcome), and PRD §7.7's "feels instant" stance already established this exact pattern for tension — recomputing progress locally from the same shared constants is consistent with that, not a new precedent. The server's own `fight.progress` remains the sole authority over whether a catch actually lands; the client's copy is display-only and can drift slightly under packet loss with no gameplay consequence.
  - **Did not rename `isInBand`'s parameters or add a `fish`/`box`-specific variant.** It's a generic symmetric range check with two legitimate call orderings; keeping it generic avoids two near-duplicate functions for no behavioral difference.
- Blocked/Open: none architecturally. Motion constants, catch box width, and progress gain/decay rates are all still unvalidated placeholders pending the next Studio pass — same open item as before, now against the corrected mechanic.
- Next: Giahy Studio pass on the corrected fishing feel (does chasing the fish with the box feel right; does the progress bar now visibly fill/drain). Cooking's real slicing mechanic remains blocked on M18 assets.

## 2026-08-06 — M5 serve verb + economy faucet (Sonnet)
- Context: M4 merged to `main` (PR #13) while this session was starting up. Giahy chose to proceed straight to M5 rather than wait on the M3 fishing-feel Studio pass, matching the precedent M4 already set of moving ahead of a not-yet-passed feel gate.
- Done (branch `claude/sushi-m5-economy`): `PlateValueResolver.lua` (new, `ServerStorage/Modules/`) implements PRD §5's full formula — `cut_base × cooking_extraction × freshness_polish × dry_age_mutation` — as a pure, headlessly-tested module (`tests/PlateValueResolver.spec.luau`), same split as `FishingCatch.lua`/`ConversionModule.lua`. `FishTable.lua` now authors a graded `cut_base[species][grade]` lookup for the 5 species `FishSpecies.lua` currently has. `EconomyConfig.lua` gained the freshness-decay/dry-age-baseline/serve-debounce constants. Two new RemoteEvents (`Player_ServePlate`, `Economy_PlateResolved`) added to `default.project.json`. `EconomyService.server.lua`: `_resolveCook` now sends each cooked portion's server-assigned id to the client (needed so the serve verb can address one portion at a time); new `_onServePlate` handler validates/debounces, resolves plate value, awards gold, removes the portion, and returns `{plateValue, breakdown}` for receipt display. `BoatCookController.lua` gained a "Serve (F)" button that drains cooked portions one press at a time. `rojo build`, `selene src`, `stylua --check src`, and all 6 `tests/*.spec.luau` files verified locally before pushing.
- Decisions (with why):
  - **`freshness_polish` and `dry_age_mutation` both got placeholder implementations rather than waiting for M6/M15.** PRD §5's formula has four terms and M5's exit criteria explicitly says "all 4 multipliers server-side" — leaving two hardcoded at 1.0 would technically satisfy "wired" but silently violate the formula. `freshness_polish` is a linear decay from the portion's own `cookedAt` clock (placeholder curve, same status as every other M3/M4 tuning number, standing in for M6's real `SpoilageService` tick). `dry_age_mutation` defaults to its documented baseline (1.0, "no mutation") since M15's aging locker doesn't exist yet — `PlateValueResolver.resolve` already accepts an override input, so M15 only has to supply a rolled number later, not touch this module.
  - **`FishTable.lua` stayed at 5 species, not PRD's ~10-species target.** Expanding the species roster is a content-authoring decision (new catch weights, prep tiers, loin counts) that affects `FishingCatch`'s weighted roll table too — inventing 5 more species solo, mid-systems-task, would be a bigger unilateral call than this session should make. Flagged in TASKS.md as a follow-up rather than silently deferred.
  - **`Player_ServePlate`'s payload is `{portionId}`, not the PRD stub's `{fishId}`.** The remote was speced before the cook verb's design (cooked output = portions with their own ids, not raw fish) existed; addressing a specific `cookedPortions` entry is what the verb actually needs to resolve, same kind of naming correction as fishing's `targetCenterAt` → `fishPositionAt` rename.
  - **Sending `breakdown` to the client in `Economy_PlateResolved` does not violate the anti-spoof invariant.** PRD §5 says the client never *computes or supplies* the multiplier components; it says nothing against the server sending its own already-resolved breakdown back for receipt display, and PRD §7.2's own remote schema names `{plateValue, breakdown}` explicitly.
- Blocked/Open: Studio-unverified — no playtest pass yet on the serve button, gold award, or the freshness-decay curve's feel (600s placeholder window, arbitrary). `FishTable.lua`'s cut_base numbers and `EconomyConfig.lua`'s freshness/dry-age constants are all unvalidated placeholders pending Giahy's M7 numbers session, same status as every other tuning file in this repo.
- Next: Giahy Studio pass on M5 (does serving feel/read right, do the placeholder gold numbers feel sane at all) — same open item as M3's fishing feel and M4's cook feel, now against a third verb. M6 (basic spoilage + slice UI) is next in the module sequence once this is Studio-verified.

## 2026-09-04 — Session reconciliation + M6 basic spoilage tick + FreshnessUI (Sonnet)
- Context: new session start. `TASKS.md` had drifted — M3/M4/M5 still showed feature-branch statuses (`in-progress`/`review`) though PRs #12–#14 were already merged to `main`; reconciled to `done` with their Studio-verification gaps preserved. Giahy chose to proceed straight into M6 without waiting on M5's Studio playtest pass, matching the precedent M4→M5 already set.
- Done (branch `claude/sushi-m6-spoilage`): New pure `SpoilageCalculator.lua` (`ServerStorage/Modules/`, same "not in PRD §7.1's file list, same deviation as PlateValueResolver.lua" shape) — `classify(elapsedSeconds, tuning) -> state, freshnessFraction`, a two-threshold fresh/stale/spoiled read with a linear display fraction; headless-tested (`tests/SpoilageCalculator.spec.luau`). `SpoilageService.server.lua` now runs a `task.spawn` loop on `EconomyConfig.SPOILAGE_TICK_INTERVAL_SECONDS` (5s) sweeping every online player's `data.inventory` (keyed on `caughtAt`) and `data.cookedPortions` (keyed on `cookedAt`), removing anything `classify` calls spoiled before snapshotting the survivors and firing the new `Spoilage_InventoryUpdate` remote. `FreshnessUI.lua` renders that snapshot (color-coded fresh/stale rows for both lists) plus a running gold total via a new `Economy_GoldUpdate` remote — fired by `PlayerDataService.server.lua` on join (initial balance) and by `EconomyService.server.lua` after each serve (mirrors the existing `Economy_PlateResolved` call site). Both new remotes added to `default.project.json`. `EconomyConfig.lua` gained the raw-fish/cooked-portion threshold constants plus the tick interval. `Bootstrap.client.lua` now requires and inits `FreshnessUI`. `rojo build`, `selene src`, `stylua --check` (all touched files), and all 7 `tests/*.spec.luau` files verified locally before pushing.
- Decisions (with why):
  - **Freshness needs no new schema field or offline catch-up bookkeeping.** Both `caughtAt` and `cookedAt` already exist on their respective entries, so `classify(now - timestamp, tuning)` is correct whether the player has been online continuously or just reconnected after a long gap — the file header's "must keep ticking while the owning player is offline" requirement falls out for free from using a pure elapsed-time function instead of a per-tick decrement. No `PlayerDataSchema` migration needed for M6.
  - **Spoiled entries are removed before the client ever sees them, not rendered as a third "spoiled" state.** `_sweepSpoiled` runs before `_snapshotInventory` builds the payload, so `FreshnessUI` only ever needs a fresh/stale color map — simpler than a three-state UI for a state that's supposed to mean "already gone."
  - **Cooked-portion thresholds are pinned relative to `EconomyConfig.FRESHNESS_DECAY_WINDOW_SECONDS`** (the point M5's `freshness_polish` already bottoms out at `CLAMP_FRESHNESS_MIN`) rather than picked independently — a portion keeps some sale value for its entire "stale" phase and is only tossed once it would already be resolving at the floor multiplier anyway. Raw-fish thresholds have no existing constant to anchor to, so they're a fresh placeholder guess, same status as every other M3–M5 tuning number.
  - **The full otoro→chutoro→akami grade-downgrade-then-toss chain (PRD §4 "Expiry downgrades, it does not destroy") was explicitly NOT built here.** M6's module-table scope is "basic freshness tick," and the downgrade chain is a materially bigger mechanic (a portion's `grade` field would need to mutate over time, which nothing downstream currently expects) that reads as M8 "real decay rates" territory — flagging the gap in `SpoilageCalculator.lua`'s own header rather than quietly building a fraction of it.
  - **The tick is periodic (5s), not event-driven.** Firing `Spoilage_InventoryUpdate` immediately after every catch/cook/serve would make the UI feel more responsive, but "basic" scope and the existing precedent (M3's progress bar, M5's plate receipt) both tolerate a short client-visible lag; noted as a nice-to-have, not a requirement, for a later pass.
- Blocked/Open: Studio-unverified — no playtest pass yet on the tick actually removing stock, the FreshnessUI panel's placement/readability alongside `BoatCookController`'s HUD, or whether the placeholder spoilage windows (5/10 min raw fish, 10/20 min cooked portions) feel right at all. Same "Studio pass needed" queue item as M3/M4/M5, now against a fourth system — added to `TASKS.md`'s Giahy queue as one consolidated line rather than four separate ones.
- Next: Giahy Studio pass covering M3's corrected fishing roles, M5's serve verb, and M6's spoilage tick + panel together (they've been stacking up unverified since 2026-08-06) — that playtest is the real gate before M7's numbers session, not any more solo backend work. Separately: two mem0 hygiene items surfaced this session, not part of M6 — the account still reports "Agent Mode / unclaimed" (Giahy should run `mem0 init --email <email>` at his convenience), and a stray `MEM0_CROSSCHECK_MARKER` memory (leftover test artifact, contains a "secret test phrase") is sitting in the `default`/`GitHub` scope from a prior persistence test — worth deleting, not a Sushi Sea decision.

## 2026-09-04 — M7 quick-pass numbers session + M8 storage tiers + M9 offline bank (Sonnet)
- Context: same-day continuation. M7 (economy tuning) was next per ROADMAP/TASKS, but its own skeleton doc (`docs/design/economy-model-skeleton.md`) is explicit that filling in its TBD variables "would be a unilateral resolution of Open Thread #3, which PRD §3/§11 forbid" — not a task an agent can just build through. Surfaced the fork to Giahy (numbers session now vs. skip ahead to a disconnected module vs. pause for Studio verification); he chose a quick numbers pass. Proposed a concrete storage-tier ladder + tier-0 decay thresholds grounded in PRD §4's "early game a few hours, late game 12h+" target; Giahy confirmed with "keep going."
- Done (branch `claude/sushi-m6-spoilage`, continued): **M7 (partial):** `docs/design/economy-model-skeleton.md` Rows 3-4 and PRD §12 Threads #3/#5 updated with the decided storage-capacity ladder and decay thresholds; everything else (WAGE_RATE, offline throughputCap, other Purchasing categories, the full progression-stage table) explicitly left open and flagged as inert-until-M11 or a follow-up exercise, not silently defaulted. **M8:** `PlayerDataSchema` gained a `storage = { tier: number }` section (schema v2→v3, migration + round-trip headless-tested); `EconomyConfig.STORAGE_TIERS` is the 4-tier ladder (capacity 10/20/40/80, spoilage multiplier 1×/2×/4×/8×, cost 0/500/3000/15000g); `SpoilageService.server.lua` now scales its base tuning by `EconomyConfig.STORAGE_TIERS[data.storage.tier].spoilageMultiplier` per player instead of using one fixed constant; `EconomyService._writeCaughtFishToInventory` refuses a catch once `#data.inventory >= capacity` (reuses the existing nil-return "can't be cooked" path, no new client-facing failure state); a new `Player_PurchaseStorageTier`/`Storage_TierUpdate` remote pair (handled in `EconomyService.server.lua`, alongside its other Player_* verb handlers per the same "no dedicated service file" precedent M4/M5 already set) lets a player spend gold to advance one tier; `PlayerDataService.server.lua` pushes the current tier on join. `FreshnessUI.lua` gained a storage line (`name (count/capacity)`) and an upgrade button that hides itself at max tier. **M9:** new pure `OfflineBankCalculator.compute` (`ServerStorage/Modules/`) implements PRD §7.4's algorithm exactly — `platesServed = min(throughput × elapsedHours, remainingStock)`, `netBank = max(0, gross - wages)` — with an explicit `staffHeadcount <= 0 → return 0` short-circuit; headless-tested (`tests/OfflineBankCalculator.spec.luau`: zero-stock, full-spoil, wage-exceeds-gross, long-elapsed, the headcount-0 case, and a normal gross-minus-wages resolution). Wired into `PlayerDataService.server.lua`'s `PlayerAdded` handler: credits gold once per join when a prior snapshot exists, then re-stamps `offlineSnapshotAt` so a repeat load before the next save can't double-credit the same window. `rojo build`, `selene src` (0 warnings after dropping an now-unused `player` param), `stylua --check` (all touched files), and all 9 `tests/*.spec.luau` files verified locally.
- Decisions (with why):
  - **Proposed numbers instead of asking Giahy to generate them from scratch, and proposed instead of picking them myself.** PRD §3/§11 forbid an agent unilaterally resolving Open Thread #3 — but Giahy explicitly opted into a "quick pass," which only works if the legwork (grounding candidate numbers in §4's stated coast-length targets, doing the multiplier-ladder arithmetic) is already done for him to approve or edit. This is different in kind from silently picking values with no sign-off.
  - **Offline bank nets to 0 by declaration, not by leaving `WAGE_RATE`/`throughputCap` as guessed placeholders.** PRD §4 says NPC staff run the restaurant "while offline" — with `staffHeadcount` always 0 today (M11 doesn't exist), there is definitionally nobody to run it, so `compute`'s short-circuit is a correct reading of the current game state, not a stand-in number pending a future fix. This also meant M9 didn't need any of the still-open M7 numbers to be implemented correctly today.
  - **Raw-fish spoilage during the offline gap needed no new code in `OfflineBankCalculator` or a "catch-up" pass.** `SpoilageService`'s existing elapsed-time-since-`caughtAt` classification (M6) already produces the correct answer the instant a reconnecting player's data is swept — PRD §7.4's step 2 ("compute spoilage... marked lost") falls out of M6's design for free, so M9's module only needed to implement steps 3-6 (the plates/wages/netBank arithmetic).
  - **Storage tier is its own `PlayerData.storage` section, not folded into `restaurant`.** PRD §5's sink stack already lists "storage capacity" and "restaurant tiers" as separate Purchasing categories (the latter is the brick-and-mortar upgrade line, unrelated to spoilage) — conflating them into one `tier` field would make a future M11 restaurant-tier upgrade silently also change spoilage rates, which nothing in the design calls for.
  - **The storage upgrade button lives in `FreshnessUI.lua`, not a new `ShopUI.lua` implementation.** `ShopUI`'s PRD-described scope ("Purchasing skill storefront") reads as the eventual home for rods/boats/equipment/restaurant-tier purchases too — building a whole storefront pattern now for one number would likely need reworking once those land; `FreshnessUI` is already the inventory-capacity-facing panel, so the tier/capacity readout and its one action fit there today.
- Blocked/Open: everything M8/M9 touch is Studio-unverified, same as M6. `tierUpgradeCost` and the tier-0 decay thresholds are first-pass numbers explicitly pending a real playtest read, not locked. `WAGE_RATE`, offline `throughputCap`, and the remaining Purchasing-category costs stay open, tracked as a single follow-up line in `TASKS.md`'s Giahy queue rather than blocking M8/M9's Definition of Done today (they're inert until M11).
- Next: the Giahy queue now has two real gates stacked up — a Studio pass (M3/M5/M6/M8's mechanics and feel) and the deferred numbers-session follow-up (WAGE_RATE, throughputCap, remaining Purchasing costs, the Thread #3 validation table). M10 (customer lifecycle) and M11 (staff) are the next code modules, but M11 specifically is what finally makes M9's offline-bank formula produce a nonzero result worth checking.

## 2026-09-04 — M10 customer lifecycle + M11 restaurant tier/staff (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." Unlike M7-M9, M10/M11 had no PRD formulas waiting to be filled in — seating capacity, staff hiring costs, staff performance-by-rarity, and wages were genuinely undecided (Thread #6 + PRD §4 "Staff"). Proposed a concrete restaurant-tier ladder, staff-rarity table, and customer-flow pacing (same "propose, Giahy confirms" pattern M7 established); Giahy confirmed with "just keep going... attempt to one shot this game." Scoped explicitly to exclude prestige/traffic math — that's M12's job per TASKS.md, not M10/M11's.
- Done (branch `claude/sushi-m6-spoilage`, continued): New `RestaurantConfig.lua` (`shared/Config/`) holds the whole M10/M11 numbers proposal: 3-tier restaurant ladder (4/8/16 seats, 2,000/10,000/50,000g), 3 staff rarities (common/rare/legendary — hire cost, base performance, tenure bonus cap), a flat `WAGE_RATE_PER_HOUR_PER_STAFF`, and placeholder customer-flow pacing constants. New pure `CustomerFlow.lua` implements the 6-stage state machine (arrival→ordering→fulfillment→eating→payment→rating) as a `tick(customer, now, hasPlateReady, tuning) -> (customer, event?)` function, headless-tested for every stage transition including the fulfillment-timeout walkout. New pure `StaffPerformance.lua` resolves a deterministic `[0,1]` performance score from rarity + tenure (linear ramp to a cap over `TENURE_SECONDS_FOR_FULL_BONUS`), headless-tested. `PlayerDataSchema` bumped to v4: `restaurant.staffHeadcount` (bare int) is replaced by `restaurant.staff` (a real roster of `{id, rarity, hiredAt}`), migration-tested both for a fresh v3 save and for the full v1→v4 and v2→v4 chains. `CustomerService.server.lua` ticks every player with `restaurant.tier > 0`: spawns customers when seats are free (rate scales with seat count, still a flat placeholder not the real traffic stat), advances each via `CustomerFlow.tick`, and on `"served"` pops+resolves the oldest `cookedPortions` entry through the exact same `PlateValueResolver` call the boat serve verb makes — one shared kitchen output, not a parallel restaurant stock pool. `StaffService.server.lua` handles `Player_PurchaseRestaurantTier` and `Player_HireStaff`, and a tick that deducts live wages (clamped at 0 gold) and auto-cooks one raw fish per staff member per tick via the SAME `ConversionModule.cook` the player's manual verb calls (§7.6), using `StaffPerformance.resolve` to synthesize the `performance` table instead of real trace/stroke input. `RestaurantUI.lua` filled in: tier/upgrade button, staff count/hire buttons (one per rarity), live customer stage list. `OfflineBankCalculator`'s `staffHeadcount` input now reads `#data.restaurant.staff` (a real number, finally) instead of the old always-0 field. `rojo build`, `selene src`, `stylua --check` (all touched files), and all 11 `tests/*.spec.luau` files verified locally before pushing.
- Decisions (with why):
  - **Prestige/rating math was deliberately left as a no-op stub.** `CustomerFlow`'s "rating" stage still fires a `"left"` event on schedule, but `CustomerService` doesn't touch `restaurant.prestigePoints` when it fires — PRD's Yelp prestige formula is still Thread #6, unset, and TASKS.md already assigns "Prestige + traffic" to M12. Writing even a rough placeholder formula here would be inventing M12's answer inside M10's scope.
  - **Customers draw from the same `cookedPortions` pool the boat serve verb drains, not a separate restaurant stock.** PRD never describes two kitchens — "the supply chain IS the game" reads as one shared output competing across whatever's demanding it (a customer, or the player manually serving on the boat). Inventing a parallel stock pool would be a bigger unilateral mechanic decision than this session should make solo.
  - **`restaurant.staff` replaces `staffHeadcount` outright, not alongside it.** Deterministic per-staff performance needs each member's own `rarity`/`hiredAt` — keeping a redundant bare counter next to the real roster would be exactly the "divergent constant" dual-source-of-truth this codebase's own comments elsewhere warn against (see `EconomyConfig.lua`'s header on not duplicating `MAX_COOKING_LEVEL`).
  - **Tenure is wall-clock time since hired, not played-time.** PRD's "the longer you keep them" doesn't specify which; wall-clock reuses the exact elapsed-time-since-a-timestamp pattern every other clock in this repo already uses (freshness, spoilage, offline bank) instead of inventing a second time-tracking mechanism just for staff.
  - **Wages deduct live, every tick, clamped at 0 — not just reconciled via the offline bank.** PRD §5 lists wages as a continuous sink ("low early, a real line item at brick-and-mortar tier"), and `OfflineBankCalculator` only covers the logged-out window; without a live deduction, an online player with staff would never actually pay them.
  - **Auto-cook throughput scales with headcount (one fish per staff member per tick), not a flat one-per-tick regardless of hires.** PRD §4 names kitchen throughput as staff-driven ("hire cooks... who perform cooking and serving automatically") and the primary bottleneck — a flat rate would make hiring a second cook pointless, contradicting the design's own headcount economy.
- Blocked/Open: everything M10/M11 touch is Studio-unverified, same as M6-M9. All of `RestaurantConfig.lua`'s numbers (seats, tier costs, hire costs, base performance, tenure curve, wage rate, spawn/stage timing) are first-pass, explicitly pending a real playtest read — same status as every other tuning file. `OfflineBankCalculator`'s `throughputPerHourWhenStaffed`/`avgPlateValueAtLogout`/`remainingStockAfterSpoilage`/`wageRatePerHourPerStaff` inputs are STILL hardcoded to 0 in `PlayerDataService.server.lua` even though `staffHeadcount` is now real — meaning a staffed player logging off and back on nets 0 offline gold today, which is wrong once M11 is meant to matter. Flagging as the next concrete gap, not silently correct.
- Next: wire real values into those four `OfflineBankCalculator.compute` inputs at `PlayerDataService.server.lua`'s call site (needs a per-tier `throughputPerHourWhenStaffed`, which doesn't exist in `RestaurantConfig.lua` yet, plus `avgPlateValueAtLogout`/`remainingStockAfterSpoilage` snapshotting at save time) — noted as a gap, not blocking the "one-shot" push through M12+.

## 2026-09-04 — M12 prestige + traffic stat (Sonnet)
- Context: same-day continuation. M12 closes out the Phase 4 (restaurant autonomy) module set — its two PRD §12 Thread #6 items (Yelp prestige formula, hidden traffic stat formula) got the same "propose numbers, keep going" treatment as M7/M10/M11.
- Done (branch `claude/sushi-m6-spoilage`, continued): New pure `TrafficStat.lua` — `starsFor(prestigePoints, tuning)` (continuous 1-5 scale, `PRESTIGE_POINTS_PER_STAR` linear climb, clamped) and `multiplierFor(stars, hospitalityLevel, cosmeticsScore, tuning)` (1 + weighted normalized inputs, clamped to `[MIN, MAX]_TRAFFIC_MULTIPLIER`); both headless-tested (`tests/TrafficStat.spec.luau`). `RestaurantConfig.lua` gained the prestige/traffic constants (points-per-served-customer, points-per-star, three weights, Hospitality normalization ceiling, multiplier clamps). `CustomerService.server.lua`: a served customer's payment now also increments `restaurant.prestigePoints` (never decremented anywhere, satisfying "never drops" by construction rather than by a guard); the spawn interval is divided by the resolved traffic multiplier instead of M10's flat per-seat rate; `Restaurant_CustomerUpdate`'s payload gained `prestigePoints`/`stars`. `RestaurantUI.lua` renders a "Yelp: x.x / 5.0" line. `rojo build`, `selene src`, `stylua --check`, and all 11 `tests/*.spec.luau` files verified locally.
- Decisions (with why):
  - **Cosmetics and Hospitality inputs are wired but always inert today**, not omitted. `TrafficStat.multiplierFor` takes a real `cosmeticsScore` parameter — `CustomerService` just always passes 0, since no cosmetics system exists anywhere in this codebase yet. Hospitality reads the player's real skill level, but that's always 1 today (`SkillConfig.lua` is still an empty stub, no XP system exists). Wiring the full three-input formula now means whichever system lands a cosmetics or leveling mechanic later only has to start producing a real number, not touch this module — same pattern `WAGE_RATE` followed before M11 existed to make it non-zero.
  - **Stars are a continuous float, not integer steps.** Real Yelp-style ratings show fractions (e.g. "4.3 stars"); a discrete 1-5 integer would make `multiplierFor`'s traffic response jump in hard steps at each threshold instead of scaling smoothly with every served customer.
- Blocked/Open: Studio-unverified, same as every module this session. All of `TrafficStat`'s weights/`PRESTIGE_POINTS_PER_STAR` are first-pass, pending a real playtest read on how fast prestige should climb relative to how often a player actually serves customers at gray-box pacing.
- Next: Phase 4 (M10-M12) is now code-complete. M13 (weather) is next — its only real dependency is M2 (done), so it's the first module since M9 that isn't gated on anything from this session's own numbers proposals.

## 2026-09-04 — M13 weather + M14 legendary encounter (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." PRD §7.5 frames weather and legendary encounters as "one system, both purposes," so built together rather than sequentially like M10/M11.
- Done (branch `claude/sushi-m6-spoilage`, continued): New `WeatherConfig.lua` — a storm catalog (one entry: `ink_storm` → `kraken`), zone-grid size (200 studs; no real world/zones exist yet, so a zone is grid-quantized cast coordinates, flagged explicitly), rotation/start-chance constants, legendary odds (baseline × in-zone multiplier), and the M14 phase-scaling constants. New pure `WeatherRoll.lua` (`zoneFor`, `randomZone`, `rollInRange`, `shouldStartStorm`, `shouldTriggerLegendary` — all injectable-randomness, same shape as `FishingCatch.rollBiteWaitSeconds`) and `LegendaryFight.lua` (`catchBoxWidthFor` narrows geometrically per phase then widens back by Fishing-level relief, `isFinalPhase`), both headless-tested. New `WeatherAccess.lua` (cross-service singleton, mirrors `PlayerDataAccess.lua` exactly) lets `EconomyService.server.lua` read the active storm without a hard dependency on `WeatherService.server.lua`. `WeatherService.server.lua` ticks every `WEATHER_ROTATION_TICK_SECONDS`, expiring/starting storms and broadcasting `Weather_StormBroadcast` (PRD §7.2's exact payload) to everyone, including late joiners. Added `kraken` as the first `legendary`-rarity `FishSpecies` entry (`catchWeight = 0`, confirmed via `FishingCatch.rollCatch`'s weighted-sum math that a zero-weight entry can never be selected normally) plus its `FishTable.CUT_BASE` row. `EconomyService.server.lua`: casts now stash their zone (`WeatherRoll.zoneFor`) for the bite timer to check against the active storm; a bite in an active storm's zone rolls a legendary encounter (odds = baseline × multiplier, zero otherwise — "weather-triggered, not summonable" read literally, no ambient chance outside that condition); `_endFight` branches — a non-final phase's catch advances to the next (harder) phase via the new `Fishing_LegendaryPhaseAdvanced` remote instead of resolving, while a final-phase catch or a failure at any phase ends the whole encounter (no partial credit). `_scheduleTimeout`/`_endFight` needed a forward declaration for their new mutual recursion (same shape `BoatCookController.lua`'s trace/stroke chain already uses). `FishingController.lua` now renders whatever `catchBoxWidth`/`fightDurationSeconds` the server sends per bite/phase-advance instead of always reading `FishingConfig.CATCH_BOX_WIDTH`, so a legendary phase's narrower box actually renders narrower. `WeatherClient.lua` shows a storm banner with a countdown. `rojo build`, `selene src`, `stylua --check`, and all 13 `tests/*.spec.luau` files verified locally.
- Decisions (with why):
  - **A "zone" is grid-quantized cast coordinates, not a named place.** No `Workspace` mapping exists in `default.project.json` and no boat/world geometry is versioned anywhere in the repo (confirmed at session start) — a real sector system needs a world to attach it to. Grid math on the location already sent in `Player_CastLine` is the smallest thing that makes "per-player server-side roll-table modification in-zone" (PRD §7.5) testable today, explicitly flagged as a placeholder in `WeatherConfig.lua`'s header for whenever real zones exist.
  - **"Weather-triggered, not summonable" is read as a hard requirement, not just the common case.** PRD §4 has one line suggesting "baseline odds near-zero-but-nonzero outside the right weather" and another flatly stating legendaries are weather-triggered — those are in tension. Picking the stricter, self-consistent reading (zero chance with no active storm in the cast's zone) avoids a confusing edge case (a legendary roll succeeding with no `legendaryType` to assign) and matches the more specific, repeated framing.
  - **Legendary phases reuse `FishingCatch`'s existing simulation, not a new one.** PRD §4 says this explicitly ("not a bespoke combat system — fishing turned up to 11") — `LegendaryFight.lua` only computes a harder `FightConfig` per phase; every phase still runs through the same `newFight`/`applyReelInput`/`tick` a normal catch uses.
  - **A failed phase — at any point, including phase 2 or 3 — ends the whole encounter with zero credit, not a partial/discounted catch.** PRD §4: "no buy-in, no loss penalty. Losing costs nothing but the moment." Any partial-credit design would be a bigger invented mechanic than the module calls for.
- Blocked/Open: Studio-unverified, same as every module this session — the storm banner, the legendary bite triggering, the phase-advance rendering, and the whole reel-through-3-phases flow have never been played. All of `WeatherConfig.lua`'s numbers (storm frequency/duration, legendary odds, phase count/narrowing/duration, level relief) are first-pass, same status as every other tuning file.
- Next: M15 (dry-aging locker) is next, deps M8 (done). Its numbers (aging duration, mutation odds/magnitude) are another first-pass-proposal situation like M10/M11/M12/M13's.

## 2026-09-04 — M15 dry-aging locker (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." M15's numbers (locker tiers, aging peak/duration, mutation odds) got the same first-pass-proposal treatment as every module since M10 — proposed and built in one pass rather than stopping to ask again, per the standing "keep going" direction.
- Done (branch `claude/sushi-m6-spoilage`, continued): New `AgingConfig.lua` — 3-tier locker ladder (2/4/8 slots, 1,500/6,000/25,000g), an aging curve (1.0 → 1.5× over 6h, then flat — "past peak the fish doesn't ruin"), and a rare mutation roll (5% chance, 1.05-1.5× bonus, "percentage multipliers, never orders of magnitude"). New pure `DryAgingLocker.lua` (`agingMultiplierFor`, `rollMutationBonus` — two independent rolls so magnitude isn't biased by the chance roll, `resolveDryAgeMutation`), headless-tested. `PlayerDataSchema` bumped to v5 (`agingLockerEquipment.tier`, migration-tested) and gained an optional `dryAgeMutation` field on `InventoryFish`/`CookedPortion` (no migration needed — nil-safe by construction, same as every optional field this schema already carries). `EconomyService.server.lua`: `Player_PurchaseAgingLockerTier` (same pattern as the storage/restaurant tier purchases), `Player_PlaceInAgingLocker` (moves a fish from `inventory` to `agingLocker`, assigning the lowest free slot number), `Player_PullFromLocker` (resolves `DryAgingLocker.resolveDryAgeMutation` once against the fish's aged time, stamps it, and re-inserts into `inventory` with a fresh `caughtAt` — it re-enters the spoilage track, per PRD, rather than carrying a stale one). `_resolveCook` and `StaffService._autoCook` both now carry `fish.dryAgeMutation` onto the portions they produce; `_onServePlate` and `CustomerService._resolveNextPlate` both now pass `portion.dryAgeMutation` into `PlateValueResolver.resolve` instead of always leaving it nil. `FreshnessUI` gained a read-only "Aging Locker: n/slots" line. `rojo build`, `selene src`, `stylua --check`, and all 14 `tests/*.spec.luau` files verified locally.
- Decisions (with why):
  - **No interactive place/pull UI was built.** M15's ROADMAP/TASKS description is about the mechanic (aging track, slot management, cash-out timer, mutation roll), not a UI requirement, unlike M6's explicit "slice UI" or M10/M11's UI call-outs — same precedent M9's offline bank set (mechanic complete, UI deferred). The remotes are fully wired and testable; a future art/UI pass adds the buttons.
  - **Pulling a fish re-stamps `caughtAt` to now, re-entering the spoilage track fresh.** PRD §4 says aging fish "leave the spoilage track and enter the aging track" — the natural reading of "enter"/"leave" is that spoilage simply doesn't apply while aging (confirmed: `SpoilageService` never reads `agingLocker`), and re-joining that track on exit should start clean rather than carrying a spoilage clock that silently ran the whole time it wasn't supposed to be running.
  - **The mutation roll happens once, at pull time, not continuously while aging.** PRD's own wording — "a full random mutation rolls on aging" — reads as one roll tied to the aging process's conclusion, not a repeated tick; a continuous roll would also need a "when do you lock it in" rule PRD never specifies.
  - **Two independent random calls (did-it-happen, then magnitude) inside `rollMutationBonus`**, not one roll reused for both. Reusing one roll would correlate magnitude with the chance threshold's own distribution (values just under 5% would cluster near the low end of the bonus range) — independent rolls keep "rare" and "how big" as separate, honestly-uniform questions.
- Blocked/Open: Studio-unverified, same as every module this session. Locker tier costs, the aging peak window (6h), and the mutation chance/range are all first-pass, pending a real playtest read — same status as every other tuning file. No UI for placing/pulling fish yet (see decision above).
- Next: M16 (trophy mounts + gifting) is next, deps M14 (done).

## 2026-09-04 — M16 trophy mounts + gifting (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." Smaller scope than the last several modules — mostly data manipulation over existing structures (the `Trophy`/`restaurant.trophies` schema fields have existed since the original scaffold, unused until now) plus one level-gate check, so no new pure module was warranted.
- Done (branch `claude/sushi-m6-spoilage`, continued): `CookConfig.MIN_COOKING_LEVEL_FOR_LEGENDARY_BUTCHER` (first-pass placeholder, level 5) gates cooking a legendary-rarity fish — checked in both `EconomyService._onCookTrace` (the manual verb) and `StaffService._autoCook` (auto-cook re-inserts the fish rather than destroying it if the gate isn't met, same "content gap, put it back" pattern already used for unknown species). `Player_MountTrophy` validates a fish is legendary rarity, removes it from `inventory`, and appends `{species, mountedAt}` to `restaurant.trophies` — decay-free by construction, since `SpoilageService` has never iterated that array. `Player_GiftFish` validates a fish is rare-or-legendary rarity, the target player is online and loaded, and the target's storage has room, then moves the fish (including any `dryAgeMutation`) from sender's `inventory` to the target's. `Restaurant_TrophyUpdate` pushes the trophy list on join and after a mount; `RestaurantUI` shows a read-only count. `rojo build`, `selene src`, `stylua --check`, and all 14 `tests/*.spec.luau` files verified locally (no new test file — nothing here is pure math worth a dedicated headless unit, matching the project's own "don't over-abstract simple logic" stance).
- Decisions (with why):
  - **Gifting only transfers the raw fish, with no direct gold path.** PRD §4 explicitly calls out "no cheap liquidation path — no whale-to-alt gold pipe"; since a gifted fish still has to go through the recipient's own Cooking-gated cook verb and the normal serve flow to become gold, the existing skill-gated formula is already the anti-liquidation mechanism — no new logic was needed to satisfy this requirement, just not building a shortcut around it.
  - **Mounting is restricted to legendary rarity, not any catch.** PRD's own framing introduces trophies specifically as "goal-markers for catches you can't yet butcher" in the Legendary section — generalizing to all rarities would be inventing a display-case mechanic PRD never asked for.
  - **The staff auto-cook gate re-inserts the gated fish rather than skipping/destroying it.** Matches the existing "unknown species" fallback in the same function exactly — a fish that can't be processed right now is not a fish that should vanish.
- Blocked/Open: Studio-unverified, same as every module this session. No interactive mount/gift UI (server-complete, same "mechanic ships, interaction UI later" precedent M15 set). The butcher-gate level (5) and the "rare+" gifting threshold are first-pass, unvalidated.
- Next: M17 (presence aura) is next, deps M11 + M0 (both done).

## 2026-09-04 — M17 presence aura (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." PRD's own hazard note for this module ("presence-boosts-quality creates an incentive to idle... needs a shape that rewards visiting rather than parking") is the actual spec — the mitigation shape, not just the multiplier, was the real design ask.
- Done (branch `claude/sushi-m6-spoilage`, continued): New pure `PresenceAura.lua` — `multiplierFor(sessionSeconds, tuning)` exponentially decays from `PRESENCE_BASE_MULTIPLIER` (1.2×) toward `PRESENCE_MIN_MULTIPLIER` (1.0×, no bonus — never a penalty) with a configurable half-life (20 min, first-pass), headless-tested for the start/half-life/floor/monotonic-decrease properties. `StaffService.server.lua` now tracks `presenceSessionStartedAt[userId]`, reset on every `PlayerAdded` and cleared on `PlayerRemoving`; `_autoCook` multiplies `StaffPerformance.resolve`'s output by `PresenceAura.multiplierFor(now - sessionStartedAt, ...)` before building the `ConversionModule.cook` performance table — `ConversionModule`'s own `_clamp01` already ceilings anything the aura pushes above 1.0, so no new clamping was needed. `rojo build`, `selene src`, `stylua --check`, and all 15 `tests/*.spec.luau` files verified locally.
- Decisions (with why):
  - **"Presence" is a play-session proxy (time since join), not real proximity.** No world/restaurant geometry exists anywhere in the repo — confirmed back at M13's weather work and unchanged since (still no `Workspace` mapping in `default.project.json`) — so there is nothing to check literal nearness against. A session-decay curve keyed to login time directly implements PRD's own proposed mitigation shape ("diminishing over a session") without inventing location tracking a boat-only gray-box can't support yet.
  - **Only staff performance is affected, never the player's own manual cook verb.** PRD's exact wording is "applies... to nearby staff" — the player manually cooking already contributes their own hand skill; there's no aura for a player to apply to themselves.
  - **Only "quality" (the performance multiplier) was implemented from "quality (and speed)."** A speed boost would need its own throughput model layered onto `StaffService`'s existing "one fish per staff per tick" loop — a second mechanic, not a small addition — so it's flagged as an explicit, deliberate gap rather than quietly built partial or invented on the spot.
- Blocked/Open: Studio-unverified, same as every module this session. `PRESENCE_BASE_MULTIPLIER`/`PRESENCE_SESSION_DECAY_HALF_LIFE_SECONDS` are first-pass, unvalidated. Speed-side aura is not built (see decision above).
- Next: M17 closes out the numbered-module backlog through Phase 5's mechanics. M18 (asset pipeline) is next — per Giahy's earlier call this session, that means the manifest + `bpy` tooling only, not actual modeling. M19 (monetization) and a final docs/handoff pass remain after that.

## 2026-09-04 — M18 asset pipeline manifest + bpy scripts (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." Earlier this session Giahy confirmed the PRD §13 open question: gray-box through Phase 4, real Blender modeling starts at Phase 5 (M13-M18) — no early artist track. M13-M17 (Phase 5's mechanics) are now done; M18 itself is explicitly manifest-and-tooling scope, not modeling, per that same answer.
- Done (branch `claude/sushi-m6-spoilage`, continued): `docs/design/Asset Pipeline.md` — the export contract restated from PRD §9 (≤10k tris/MeshPart, ≤4 texture maps, applied transforms, origin pivot, real-world stud scale via meters-in-Blender), a naming convention (`{Category}_{AssetName}_{Variant?}`, PascalCase to match this repo's Luau module-naming style), and a content manifest built from this session's actual gray-box gaps rather than a speculative list: every `FishSpecies`/`FishTable` species with a poly budget, `Prop_CookingBoard`/`Prop_Boat_Starter`/`Prop_FishingRod_Starter`, every `RestaurantConfig.RESTAURANT_TIERS`/`EconomyConfig.STORAGE_TIERS`/`AgingConfig.LOCKER_TIERS` entry, and generic customer/staff NPC silhouettes (`CustomerService` currently has zero visual representation — state machine only). Explicitly scoped OUT: UI icons (Figma's job per PRD, not Blender), weather VFX (Studio particles/lighting, not modeled), and terrain (a `Studio Setup.md` runbook that doesn't exist yet, flagged separately). Three `scripts/blender/` tools: `validate_asset.py` (checks the export contract against a selected object — triangle count via per-polygon triangulation math, texture-map count via node-tree inspection, unapplied-transform detection, pivot-offset-from-geometry-center), `export_fbx_batch.py` (batch-exports every top-level mesh to its own `.fbx` named from the Blender outliner, so naming-convention compliance in Blender directly becomes the exported filename), `procedural_fish_scale.py` (duplicates one base fish mesh at per-species relative scale factors, letting one authored model stand in for the whole roster during an early art pass). All three syntax-checked with a local Python interpreter (`py_compile`) — no Blender install available in this environment to run them against real geometry.
- Decisions (with why):
  - **No modeling was attempted, and the manifest says so explicitly at the top.** This isn't a capability gap being quietly worked around — Giahy's own answer this session was specifically "gray-box through Phase 4, Blender starts at Phase 5," and PRD §9's stance ("specify and script, don't sculpt") has been the rule since the original PRD, not something this session is reinterpreting.
  - **The manifest is grounded in this session's real gray-box gaps** (fish species already in `FishSpecies.lua`, tier tables already in `RestaurantConfig`/`EconomyConfig`/`AgingConfig`, the specific cook-verb geometry blocker documented back in M4's BUILD_LOG entry) rather than invented from PRD prose alone — every row traces to a file or a known blocker already in this repo, so nothing here could be mistaken for a new mechanic decision dressed up as an art requirement.
  - **The cook-verb board/fish pair is called out as the highest-priority asset**, not just listed alongside everything else — it's the one gray-box mechanic in this entire codebase that's explicitly *waiting* on real geometry to be built at all (M4's "stop the marker" stand-in), unlike every other system here which is complete and just Studio-unverified.
  - **UI icons and weather VFX are explicitly excluded**, not silently omitted. PRD names Figma as the UI tool (not Blender) and Studio-native particles/lighting are the normal path for weather effects — including them here would blur this document's actual scope (3D asset modeling) with two adjacent but distinct pipelines.
- Blocked/Open: the three `bpy` scripts are unverified against real Blender — there is no Blender install in this environment, and no assets exist yet in this repo to test them against. Poly budgets throughout the manifest are first-pass guesses. The actual "done" state of M18 depends entirely on Giahy or an artist opening Blender, which is explicitly out of this session's scope.
- Next: M19 (monetization/PassManager) is next, deps M12 (done). After that: a final docs/BUILD_LOG/mem0 pass and a handoff summary — M20 (launch prep) is mostly Giahy/Studio/publishing actions this session can't perform.

## 2026-09-04 — M19 monetization / PassManager (Sonnet)
- Context: same-day continuation, "attempt to one-shot the game." This is the last numbered module this session builds — M20 (launch prep) is overwhelmingly Giahy/Studio/publishing actions (final playtest, Studio Setup.md runbook, Creator Hub publish) that can't be done from here.
- Done (branch `claude/sushi-m6-spoilage`, continued): `PassManager.lua` (was an empty M1 stub) filled in — an ownership cache (`{[userId]: {[gamePassId]: boolean}}`), `refreshForPlayer` (queries `MarketplaceService:UserOwnsGamePassAsync` once per pass per join, per PRD's explicit "cache ownership checks once per player per session"), `playerOwns`, `promptPurchase`, and a module-level `PromptGamePassPurchaseFinished` listener keeping the cache live after a purchase completes. `UserOwnsGamePassAsync` is wrapped in `pcall` — it throws on an unpublished place or the placeholder id 0, same failure shape `PlayerDataService.server.lua`'s `DataStoreService` fallback already handles, treated as "not owned" rather than erroring so the rest of the game stays testable locally. New `MonetizationConfig.lua` — one catalog entry, `RESEARCHER_HAT` (cosmetic, `gamePassId = 0` placeholder pending a real Creator Dashboard id post-publish). Wired into `PlayerDataService.server.lua`'s existing join/leave handlers: refreshes + pushes `Monetization_OwnershipUpdate` on join, releases the cache on leave, and handles a new `Player_PromptPassPurchase` remote. `rojo build`, `selene src`, `stylua --check`, and all 15 `tests/*.spec.luau` files verified locally (no new test file — this is a cache wrapper around a Roblox API, no pure math to headlessly test).
- Decisions (with why):
  - **The catalog has exactly one entry, and it's cosmetic-only.** PRD's stance is explicit ("F2P; cosmetics and convenience only... nobody is taxed for winning") and names a `reviewer-reality` check against pay-to-win as part of this module's Definition of Done. Designing a real storefront (what to sell, at what price, how many SKUs) is a product/business decision — the same category this session has routed to Giahy every time it came up (economy numbers, restaurant/staff numbers, aging numbers) rather than inventing solo. A purely cosmetic item can't be pay-to-win by construction regardless of price, which is what makes it safe to include as the one proof-of-wiring entry without needing that business-decision conversation first.
  - **PassManager stayed a module, not a new service file.** PRD §7.1 explicitly lists `PassManager.lua` under `ServerStorage/Modules` with no corresponding service — wiring its per-player lifecycle into `PlayerDataService.server.lua`'s already-established join/leave handlers (where every other cross-cutting system's join push already lives) avoids inventing an unlisted service file for what's fundamentally a few lines of glue.
- Blocked/Open: fundamentally unverifiable outside a published place — `UserOwnsGamePassAsync`/`PromptGamePassPurchase` need a real GamePass id, which needs a published experience, which is a Giahy action (HANDOFF.md, PRD). No purchase-prompt UI exists yet (remote is wired, nothing calls it client-side). The actual monetization catalog (what to sell beyond the one proof-of-concept cosmetic, at what price) remains an open product decision for Giahy.
- Next: this closes the M6-M19 backlog for this session. Remaining: a final docs/BUILD_LOG/TASKS reconciliation pass, mem0 durable-decision writes, and a handoff summary to Giahy covering what's code-complete vs. what still needs him (Studio playtest across every module, M20 launch prep, publishing, and the still-open numbers-session follow-ups from M7/M9/M11 tracked in the Giahy queue below).
